package com.kapil.preparation.java8.deusteche;

import java.util.stream.Stream;
/*
* Stream<Stream<ProcessedTransaction>>
* */
public class Reconcile {
    Stream<PendingTransactions> reconciler ( Stream<PendingTransactions> pending,
                                             Stream<Stream<ProcessedTransaction>> processed) {
        return null;
    }
}
